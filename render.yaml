services:
  - type: web
    name: malaria-api
    env: python
    plan: free
    buildCommand: pip install -r Summative/API/requirements.txt
    startCommand: uvicorn Summative.API.app:app --host 0.0.0.0 --port 10000
    runtime: python
    region: oregon
    branch: main